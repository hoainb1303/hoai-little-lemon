const Order = () => {
  return (
    <section
      style={{
        display: "flex",
        justifyContent: "center",
        background: "#495e57",
        padding: "10rem 10rem",
      }}
    >
      <h1
        style={{
          color: "white",
        }}
      >
        ORDER PAGE
      </h1>
    </section>
  );
};

export default Order;
